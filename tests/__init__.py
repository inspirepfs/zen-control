"""ZEN Control test package.

Keep the repository test suite an explicit package so clean CI environments cannot
resolve an unrelated third-party top-level ``tests`` package instead of this tree.
"""
